#pragma once

#include <string>
#include <vector>

namespace model::mde {
using TableValue = std::string;
// TODO: remove this, rename the file
using Record = std::vector<TableValue>;
}  // namespace model::mde
